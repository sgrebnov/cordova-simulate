var geo = require('./geo');

function initialize() {
    var latitude = document.getElementById('geo-latitude');
    var longitude = document.getElementById('geo-longitude');
    var altitude = document.getElementById('geo-altitude');
    var accuracy = document.getElementById('geo-accuracy');

    function updateGeo() {

    }
}

module.exports = {
    initialize: initialize
};
